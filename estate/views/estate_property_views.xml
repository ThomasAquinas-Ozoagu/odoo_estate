<?xml version="1.0"?>

<odoo>

    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Property">
                <field name="name"/>
                <field name="postcode"/>
                 <!-- 4. A Pre-defined Filter -->
                <filter string="Available" name="available" domain="[('state', 'in', ('new', 'offer_received'))]"/>
                <!-- 5. A Visual Separator -->
                <separator/>
                <!-- 6. A Group-By Option -->
                <filter string="Postcode" name="groupby_postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>


    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="PropertyForm">
                <header>
                    <button name="action_set_sold" string="Sold" type="object" state="offer_received" class="btn-success"/>
                    <button name="action_set_canceled" string="Cancel" type="object" state="new,offer_received" class="btn-danger"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,sold,canceled"/>
                </header>
                <sheet>
                    <group>
                        <h1> <field name="name"/> </h1>
                        <field name="description"/>
                        <group>
                            <field name="tag_ids" widget="many2many_tags"/>
                            <field name="property_type_id"/>
                            <field name="state"/>
                            <!-- <field name="property_type_name" readonly="1"/> -->
                        </group>
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                            <field name="expected_price"/>
                            <field name="best_offer" readonly="1"/>
                            <field name="selling_price"/>
                            <field name="offer_count" readonly="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                            </group>
                        </page>
                        <page string="Details">
                            <group>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="garden == False"/>
                                <field name="garden_orientation" invisible="garden == False"/>
                                <field name="total_area" readonly="1"/>
                            </group>
                        </page>
                        <page string="Status">
                            <group>
                                <field name="active"/>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offer_ids">
                                    <list editable="bottom">
                                        <field name="price"/>
                                        <field name="partner_id"/>
                                        <field name="validity"/>
                                        <field name="date_deadline"/>
                                        <button name="action_accept_offer" string="Accept" type="object" icon="fa-check"/>
                                        <button name="action_reject_offer" string="Reject" type="object" icon="fa-times"/>
                                        <field name="status"/>
                                    </list>
                                </field>
                            </group>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="partner_id"/>
                                <field name="seller_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>   
        </field>
    </record>

    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Property list" create="true" edit="true" delete="true">
                <field name="name"/>
<!--                <field name="tag_ids"/> -->
                <field name="property_type_id"/>
                <field name="partner_id"/>
                <field name="seller_id"/>
               <!-- <field name="property_type_name"/> -->
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability"/>
                <field name="garden"/>
                <field name="garden_area"/>
                <field name="garden_orientation"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="estate_property_view_search"/>
    </record>
</odoo>
